# ============================================
# UPDATED ROOT docker-compose.yml
# Add this to your existing ~/Projects/caffis/docker-compose.yml
# ============================================

version: '3.8'

services:
  # Your existing services (keep these as they are)
  backend:
    build: ./server
    container_name: caffis-backend
    ports:
      - "5000:5000"
      - "5555:5555" 
    volumes:
      - ./server:/app
      - /app/node_modules
    env_file:
      - .env
    depends_on:
      - db
    restart: unless-stopped

  frontend:
    build: ./client
    container_name: caffis-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./client:/app
      - /app/node_modules
    env_file:
      - .env
    depends_on:
      - backend
    restart: unless-stopped

  db:
    image: postgres:15
    container_name: caffis-db
    restart: always
    environment:
      POSTGRES_USER: caffis_user
      POSTGRES_PASSWORD: admin5026
      POSTGRES_DB: caffis_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  # ============================================
  # ADD THESE NEW SERVICES FOR MAP MICROSERVICE
  # ============================================

  # Map Service Backend
  map-backend:
    build: 
      context: ./caffis-map-service/backend
      dockerfile: Dockerfile
    container_name: caffis-map-backend
    restart: unless-stopped
    ports:
      - "5001:5001"
    environment:
      - NODE_ENV=development
      - PORT=5001
      - REDIS_HOST=map-redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=caffis-dev-redis-2025
      - JWT_SECRET=${JWT_SECRET}
      - FRONTEND_URL=http://localhost:3000
      - MAIN_API_URL=http://backend:5000
      - MAPBOX_ACCESS_TOKEN=pk.eyJ1IjoiYWxpczIwMDEiLCJhIjoiY21jcnNjODFoMHIybTJrcXNuYWw1NXZlYiJ9.dqmeeyoou2m-BngWFzG2Lw
    volumes:
      - ./caffis-map-service/backend:/app
      - /app/node_modules
    depends_on:
      - map-redis
      - backend
    restart: unless-stopped

  # Redis for Map Service
  map-redis:
    image: redis:7-alpine
    container_name: caffis-map-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --requirepass caffis-dev-redis-2025
    volumes:
      - map_redis_data:/data

  # Map Service Frontend (Development)
  map-frontend-dev:
    build:
      context: ./caffis-map-service/frontend
      dockerfile: Dockerfile
      target: development
    container_name: caffis-map-frontend-dev
    restart: unless-stopped
    ports:
      - "3002:3001"
    environment:
      - REACT_APP_MAP_API_URL=http://localhost:5001
      - REACT_APP_MAPBOX_TOKEN=pk.eyJ1IjoiYWxpczIwMDEiLCJhIjoiY21jcnNjODFoMHIybTJrcXNuYWw1NXZlYiJ9.dqmeeyoou2m-BngWFzG2Lw
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./caffis-map-service/frontend:/app
      - /app/node_modules
    depends_on:
      - map-backend

volumes:
  pgdata:
  map_redis_data:  # Add this new volume